# تطوير محلي (SQLite) — بعد التعديل:
#   npx prisma generate && npx prisma db push
DATABASE_URL="file:./prisma/dev.db"

# للإنتاج مع PostgreSQL استخدم نسخة المشروع على سطح المكتب (حي-الملتقى-موقع-20260403-1517)
# مع Docker + prisma migrate deploy.

ADMIN_PASSWORD="change_me"
ADMIN_SECRET="change_me_to_a_long_random_secret_at_least_16_chars"
